# Configuration Prefect pour le projet M2 BI
# Copiez ce fichier en .env et ajustez les valeurs selon votre environnement

# === Configuration GCP Block ===
# Nom du Block Prefect GCP Credentials créé dans Prefect Cloud/Server
# Pour créer le block : https://docs.prefect.io/integrations/prefect-gcp/
PREFECT_GCP_CREDENTIALS_BLOCK=sa-key

# === Configuration dbt ===
# Cible dbt : "dev" ou "prod"
# Convention : Local=dev, Prefect Cloud=prod
DBT_TARGET=dev

# Datasets BigQuery (schema) pour chaque environnement
DBT_BIGQUERY_DATASET_DEV=test_terraform_473818_dev
DBT_BIGQUERY_DATASET_PROD=test_terraform_473818_prod

# Nom du profil dbt (doit correspondre à dbt_project.yml)
DBT_PROFILE_NAME=projet_m2_bi

# === Informations GCP (référence) ===
# Ces variables documentent l'environnement mais ne sont pas utilisées par le flow
GCP_PROJECT_ID=test-terraform-473818
GCP_REGION=europe-west9

# === Note ===
# Pour générer automatiquement ce fichier depuis Terraform :
#   python scripts/generate_env.py
#
# Pour Prefect Cloud, configurez ces variables dans l'UI :
#   Settings > Variables > Add Variable

